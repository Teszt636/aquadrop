# Aquadrop Landing

This is a Next.js 15 project scaffold using the App Router with a `src/` directory layout.

## Available scripts

- `npm run dev`
- `npm run build`
- `npm run start`
- `npm run lint`

## Notes

Dependencies are declared in `package.json`, but installation was intentionally not run in this environment.
